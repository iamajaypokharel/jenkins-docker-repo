- name: Run Docker Compose
      env:
        DB_USER: postgres
        DB_PASSWORD: postgres_password
        DB_NAME: todo_db
      run: |
        docker compose down -v
        docker compose up --build -d
